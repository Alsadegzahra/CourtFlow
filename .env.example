# CourtFlow – example env vars (copy to .env and fill)
# Data paths (defaults: ./data, ./data/courtflow.db)
COURTFLOW_DATA_DIR=./data
COURTFLOW_DB_PATH=./data/courtflow.db

# Logging
COURTFLOW_LOG_LEVEL=INFO

# Cloud (Cloudflare R2) – see README "Cloud upload (R2)" for steps
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=courtflow
# R2_ACCOUNT_ID=

# Optional: API base URL for dashboard cloud buttons (default http://127.0.0.1:8000)
# COURTFLOW_API_URL=http://127.0.0.1:8000

# Optional: custom detection weights (e.g. trained best.pt – put file in models/ then set this)
# COURTFLOW_DETECTION_MODEL=./models/best.pt
