## B1 – Capture (Input Video → Chunks)

- **Responsibility**: Ingest a raw match video and convert it into uniformly encoded MP4 chunks for downstream processing.
- **Main code**: `recorder_ffmpeg.py`

### Tools / Libraries

- FFmpeg / ffprobe (via CLI)
- Python standard library (`subprocess`, `pathlib`)

### Models / Intelligence

- _(None in Phase 1 — pure video I/O and transcoding.)_

